VIEW_USERNAME=admin
# Required: set a strong password before starting the service.
VIEW_PASSWORD=
BIND_ADDRESS=127.0.0.1
PORT=8080
# Optional: override the container image (defaults to latest from GHCR).
# TELEMETRY_IMAGE=ghcr.io/skrashevich/enkapp/har-telemetry:latest
